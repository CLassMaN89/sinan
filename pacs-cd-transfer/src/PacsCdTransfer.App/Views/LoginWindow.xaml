<Window x:Class="PacsCdTransfer.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PACS CD Transfer — Giriş" Height="460" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="PACS CD Transfer" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextHiBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Hastane PACS Entegrasyon İstasyonu" FontSize="12" Foreground="{StaticResource TextLoBrush}" HorizontalAlignment="Center" Margin="0,4,0,28"/>

            <TextBlock Text="Kullanıcı Adı" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameBox" Text="admin" Margin="0,0,0,14"/>

            <TextBlock Text="Şifre" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10" KeyDown="PasswordBox_KeyDown"/>

            <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" FontSize="11" Margin="0,0,0,10"
                       TextWrapping="Wrap" Visibility="Collapsed"/>

            <Button Content="Giriş Yap" Style="{StaticResource PrimaryButton}" Click="LoginButton_Click" Height="38"/>

            <Button Content="Tanılama Günlüğünü Göster" Style="{StaticResource OutlineButton}" Click="ShowDiagLog_Click" Margin="0,14,0,0"/>
        </StackPanel>
    </Grid>
</Window>
